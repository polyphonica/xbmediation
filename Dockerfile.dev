# Local development only — mirrors the runtime version used by the
# production Dockerfile, but keeps deps installed at container start so
# bind-mounted source changes hot-reload via `next dev`.
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
EXPOSE 3000
CMD ["npm", "run", "dev"]
